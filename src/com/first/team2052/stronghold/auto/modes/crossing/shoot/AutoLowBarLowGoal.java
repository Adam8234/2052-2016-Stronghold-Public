package com.first.team2052.stronghold.auto.modes.crossing.shoot;

import com.first.team2052.stronghold.auto.AutoMode;
import com.first.team2052.stronghold.auto.AutoModeEndedException;

public class AutoLowBarLowGoal extends AutoMode {

	@Override
	protected void init()throws AutoModeEndedException {
		//getDriveTrain().setDistanceTrajectory(-19.8 * 12, 3 * 12);
		//waitUntilDistance(-19.8 * 12);
		//getDriveTrain().setTurnTrajectory(60);
		//waitUntilAngle(60);
		//vision tracking code to see high goal for alignment
		//getDriveTrain().setDistanceTrajectory(-8 * 12, 4 * 12);
		//waitUntilDistance(-8 * 12);
		//getShooter().wantOutake = true;
		//delay(3.0);
		//getShooter().wantOutake = false;
		//Shoot code
	}
}